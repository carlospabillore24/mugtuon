# MugTuon PHP API — route everything to index.php
RewriteEngine On

# Block direct access to sensitive files
<FilesMatch "\.(env|env\.php|log)$">
    Require all denied
</FilesMatch>

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
